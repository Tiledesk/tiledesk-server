#!/usr/bin/env node

/**
 * Module dependencies.
 */

/**
 * CLI entry: bootstrap is in ../server.js (HTTP + WebSocket listen via startListen).
 */

var app = require('../server.js');
app.startListen();
