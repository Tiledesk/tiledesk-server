"use strict";

const P = require("./permissions");

/**
 * Default permission sets per standard role name.
 * Custom roles use DB-backed permissions on project_user.permissions.
 */
const ALL = Object.freeze(Object.values(P));

const OWNER = ALL;

const ADMIN = new Set([
  P.REQUEST_READ_ALL,
  P.REQUEST_READ_GROUP,
  P.REQUEST_READ_MINE,
  P.REQUEST_SEND,
  P.REQUEST_CLOSE,
  P.REQUEST_JOIN,
  P.REQUEST_REOPEN,
  P.REQUEST_DELETE,
  P.REQUEST_UPDATE_STATUS,
  P.REQUEST_UPDATE_PRIORITY,
  P.REQUEST_UPDATE_FOLLOWERS,
  P.REQUEST_UPDATE_SMART_ASSIGNMENT,
  P.REQUEST_UPDATE_TAGS,
  P.REQUEST_UPDATE_NOTES,
  P.REQUEST_REASSIGN,
  P.REQUEST_ADD,
  P.REQUEST_LEFT,
  P.REQUEST_TRANSCRIPT_SEND,
  P.HISTORY_READ,
  P.HISTORY_FILTER_BY_AGENT,
  P.REQUESTS_EXPORT,
  P.CONVERSATION_DETAIL_READ,
  P.TICKET_ID_READ,
  P.RATING_READ,
  P.TAGS_READ_ALL,
  P.CHANGE_PROJECT,
  P.SIMULATE_CONV,
  P.QUOTA_USAGE_READ,
  P.AUTOMATIONSLOG_READ,
  P.AUTOMATIONSLOG_CREATE,
  P.KB_READ,
  P.KB_CONTENTS_ADD,
  P.KB_CONTENT_UPDATE,
  P.KB_CONTENT_REINDEX,
  P.KB_CONTENT_CHECK_STATUS,
  P.KB_NAMESPACE_ADD,
  P.KB_SETTINGS_EDIT,
  P.KB_DELETE,
  P.KB_CONTENTS_EXPORT,
  P.FLOWS_LIST,
  P.FLOW_CREATE,
  P.FLOW_UPDATE,
  P.FLOW_TEST,
  P.FLOW_DUPLICATE,
  P.FLOW_DELETE,
  P.FLOW_SHARE,
  P.FLOW_EXPORT,
  P.FLOW_WEBHOOK_COPY,
  P.FLOW_WEBHOOK_EDIT,
  P.FLOW_WEBHOOK_DELETE,
  P.LEADS_READ,
  P.LEAD_UPDATE,
  P.LEAD_RESTORE,
  P.LEAD_TRASH,
  P.LEAD_DELETE,
  P.LEADS_EXPORT,
  P.LEAD_BAN,
  P.LEAD_UNBAN,
  P.ANALYTICS_READ,
  P.ACTIVITIES_READ,
  P.WIDGETSETUP_READ,
  P.DEPARTMENT_LIST_READ,
  P.DEPARTMENT_DETAIL_READ,
  P.DEPARTMENT_CREATE_READ,
  P.TEAMMATES_READ,
  P.TEAMMATE_UPDATE,
  P.TEAMMATES_CREATE,
  P.ROLES_READ,
  P.ROLES_CREATE,
  P.ROLES_UPDATE,
  P.ROLE_DELETE,
  P.GROUPS_READ,
  P.GROUPS_CREATE,
  P.GROUP_UPDATE,
  P.GROUP_DELETE,
  P.EMAIL_TICKETING_READ,
  P.EMAIL_TICKETING_UPDATE,
  P.CANNED_RESPONSES_READ,
  P.CANNED_RESPONSES_UPDATE,
  P.CANNED_RESPONSES_CREATE,
  P.CANNED_RESPONSES_DELETE,
  P.TAGS_READ,
  P.TAG_CREATE,
  P.TAG_DELETE,
  P.TAG_UPDATE,
  P.HOURS_READ,
  P.HOURS_UPDATE,
  P.HOURS_DELETE,
  P.HOURS_CREATE,
  P.APPS_READ,
  P.APPS_UPDATE,
  P.PROJECTSETTINGS_GENERAL_READ,
  P.PROJECTSETTINGS_GENERAL_UPDATE,
  P.PROJECTSETTINGS_DEVELOPER_READ,
  P.PROJECTSETTINGS_DEVELOPER_UPDATE,
  P.PROJECTSETTINGS_SMARTASSIGNMENT_READ,
  P.PROJECTSETTINGS_SMARTASSIGNMENT_UPDATE,
  P.PROJECTSETTINGS_NOTIFICATION_READ,
  P.PROJECTSETTINGS_SECURITY_READ,
  P.PROJECTSETTINGS_BANNED_READ,
  P.PROJECTSETTINGS_ADVANCED_READ,
  P.APIKEY_READ,
  P.APIKEY_CREATE,
  P.APIKEY_UPDATE,
  P.APIKEY_DELETE,
]);

const AGENT = new Set([
  P.REQUEST_READ_ALL,
  P.REQUEST_READ_GROUP,
  P.REQUEST_READ_MINE,
  P.REQUEST_SEND,
  P.REQUEST_CLOSE,
  P.REQUEST_JOIN,
  P.REQUEST_REOPEN,
  P.REQUEST_DELETE,
  P.REQUEST_UPDATE_STATUS,
  P.REQUEST_UPDATE_PRIORITY,
  P.REQUEST_UPDATE_FOLLOWERS,
  P.REQUEST_UPDATE_SMART_ASSIGNMENT,
  P.REQUEST_UPDATE_TAGS,
  P.REQUEST_UPDATE_NOTES,
  P.REQUEST_REASSIGN,
  P.REQUEST_ADD,
  P.REQUEST_LEFT,
  P.REQUEST_TRANSCRIPT_SEND,
  P.HISTORY_READ,
  P.HISTORY_FILTER_BY_AGENT,
  P.REQUESTS_EXPORT,
  P.CONVERSATION_DETAIL_READ,
  P.TICKET_ID_READ,
  P.RATING_READ,
  P.TAGS_READ_ALL,
  P.CHANGE_PROJECT,
  P.SIMULATE_CONV,
  P.QUOTA_USAGE_READ,
  P.AUTOMATIONSLOG_READ,
  P.AUTOMATIONSLOG_CREATE,
  P.FLOWS_READ,
  P.FLOW_TEST,
  P.FLOW_SHARE,
  P.LEADS_READ,
  P.LEAD_UPDATE,
  P.LEAD_RESTORE,
  P.LEAD_TRASH,
  P.LEAD_DELETE,
  P.LEADS_EXPORT,
  P.LEAD_BAN,
  P.LEAD_UNBAN,
  P.WIDGETSETUP_READ,
  P.DEPARTMENT_LIST_READ,
  P.TEAMMATES_READ,
  P.TEAMMATE_UPDATE,
  P.TEAMMATES_CREATE,
  P.ROLES_READ,
  P.ROLES_CREATE,
  P.ROLES_UPDATE,
  P.ROLE_DELETE,
  P.GROUPS_READ,
  P.GROUPS_CREATE,
  P.GROUP_UPDATE,
  P.GROUP_DELETE,
  P.EMAIL_TICKETING_READ,
  P.EMAIL_TICKETING_UPDATE,
  P.CANNED_RESPONSES_READ,
  P.CANNED_RESPONSES_UPDATE,
  P.CANNED_RESPONSES_CREATE,
  P.CANNED_RESPONSES_DELETE,
  P.TAGS_READ,
  P.TAG_CREATE,
  P.TAG_DELETE,
  P.TAG_UPDATE,
  P.HOURS_READ,
  P.HOURS_UPDATE,
  P.HOURS_DELETE,
  P.HOURS_CREATE,
  P.INTEGRATIONS_READ,
  P.INTEGRATIONS_UPDATE,
  P.APPS_READ,
  P.APPS_UPDATE,
  P.PROJECTSETTINGS_GENERAL_READ,
  P.PROJECTSETTINGS_GENERAL_UPDATE,
  P.PROJECTSETTINGS_DEVELOPER_READ,
  P.PROJECTSETTINGS_DEVELOPER_UPDATE,
  P.PROJECTSETTINGS_SMARTASSIGNMENT_READ,
  P.PROJECTSETTINGS_SMARTASSIGNMENT_UPDATE,
  P.PROJECTSETTINGS_NOTIFICATION_READ,
  P.PROJECTSETTINGS_SECURITY_READ,
  P.PROJECTSETTINGS_BANNED_READ,
  P.PROJECTSETTINGS_ADVANCED_READ,
]);

const GUEST = new Set([
  P.REQUEST_READ_ALL,
  P.REQUEST_READ_GROUP,
  P.REQUEST_READ_MINE,
  P.REQUEST_SEND,
  P.REQUEST_CLOSE,
  P.REQUEST_JOIN,
  P.REQUEST_REOPEN,
  P.REQUEST_DELETE,
  P.REQUEST_UPDATE_STATUS,
  P.REQUEST_UPDATE_PRIORITY,
  P.REQUEST_UPDATE_FOLLOWERS,
  P.REQUEST_UPDATE_SMART_ASSIGNMENT,
  P.REQUEST_UPDATE_TAGS,
  P.REQUEST_UPDATE_NOTES,
  P.REQUEST_REASSIGN,
  P.REQUEST_ADD,
  P.REQUEST_LEFT,
  P.REQUEST_TRANSCRIPT_SEND,
  P.HISTORY_READ,
  P.HISTORY_FILTER_BY_AGENT,
  P.REQUESTS_EXPORT,
  P.CONVERSATION_DETAIL_READ,
  P.TICKET_ID_READ,
  P.RATING_READ,
  P.TAGS_READ_ALL,
  P.CHANGE_PROJECT,
  P.SIMULATE_CONV,
  P.QUOTA_USAGE_READ,
  P.AUTOMATIONSLOG_READ,
  P.AUTOMATIONSLOG_CREATE,
  P.KB_READ,
  P.KB_CONTENTS_ADD,
  P.KB_CONTENT_UPDATE,
  P.KB_CONTENT_REINDEX,
  P.KB_CONTENT_CHECK_STATUS,
  P.KB_NAMESPACE_ADD,
  P.KB_SETTINGS_EDIT,
  P.KB_DELETE,
  P.KB_CONTENTS_EXPORT,
  P.FLOWS_READ,
  P.FLOW_ADD,
  P.FLOW_EDIT,
  P.FLOW_TEST,
  P.FLOW_DUPLICATE,
  P.FLOW_DELETE,
  P.FLOW_SHARE,
  P.FLOW_EXPORT,
  P.FLOW_WEBHOOK_COPY,
  P.FLOW_WEBHOOK_EDIT,
  P.FLOW_WEBHOOK_DELETE,
  P.LEADS_READ,
  P.LEAD_UPDATE,
  P.LEAD_RESTORE,
  P.LEAD_TRASH,
  P.LEAD_DELETE,
  P.LEADS_EXPORT,
  P.LEAD_BAN,
  P.LEAD_UNBAN,
  P.ANALYTICS_READ,
  P.ACTIVITIES_READ,
  P.WIDGETSETUP_READ,
  P.DEPARTMENT_LIST_READ,
  P.DEPARTMENT_DETAIL_READ,
  P.DEPARTMENT_CREATE_READ,
  P.TEAMMATES_READ,
  P.TEAMMATE_UPDATE,
  P.TEAMMATES_CREATE,
  P.ROLES_READ,
  P.ROLES_CREATE,
  P.ROLES_UPDATE,
  P.ROLE_DELETE,
  P.GROUPS_READ,
  P.GROUPS_CREATE,
  P.GROUP_UPDATE,
  P.GROUP_DELETE,
  P.EMAIL_TICKETING_READ,
  P.EMAIL_TICKETING_UPDATE,
  P.CANNED_RESPONSES_READ,
  P.CANNED_RESPONSES_UPDATE,
  P.CANNED_RESPONSES_CREATE,
  P.CANNED_RESPONSES_DELETE,
  P.TAGS_READ,
  P.TAG_CREATE,
  P.TAG_DELETE,
  P.TAG_UPDATE,
  P.HOURS_READ,
  P.HOURS_UPDATE,
  P.HOURS_DELETE,
  P.HOURS_CREATE,
  P.INTEGRATIONS_READ,
  P.INTEGRATIONS_UPDATE,
  P.APPS_READ,
  P.APPS_UPDATE,
  P.PROJECTSETTINGS_GENERAL_READ,
  P.PROJECTSETTINGS_GENERAL_UPDATE,
  P.PROJECTSETTINGS_DEVELOPER_READ,
  P.PROJECTSETTINGS_DEVELOPER_UPDATE,
  P.PROJECTSETTINGS_SMARTASSIGNMENT_READ,
  P.PROJECTSETTINGS_SMARTASSIGNMENT_UPDATE,
  P.PROJECTSETTINGS_NOTIFICATION_READ,
  P.PROJECTSETTINGS_SECURITY_READ,
  P.PROJECTSETTINGS_BANNED_READ,
  P.PROJECTSETTINGS_ADVANCED_READ,
]);

/**
 * Implicit permissions granted to non-human callers.
 * Bot users (Faq_kb instances) and subscription users (Subscription instances)
 * do not have a project_user document, so their permissions live here.
 *
 * Start minimal: add explicit entries only when an endpoint is migrated and
 * needs to remain accessible to bots or subscriptions.
 */
const BOT_PERMISSIONS = new Set([
  P.FLOWS_READ,
]);

const SUBSCRIPTION_PERMISSIONS = new Set([
  P.FLOWS_READ,
]);

module.exports = {
  ROLE_PERMISSIONS: Object.freeze({
    owner: OWNER,
    admin: ADMIN,
    agent: AGENT,
    guest: GUEST,
    teammate: AGENT,
    user: GUEST,
    supervisor: AGENT,
  }),

  /**
   * Minimal permissions enforced when roleType === 2 (guest end-user).
   */
  GUEST_ROLETYPE_PERMISSIONS: GUEST,

  BOT_PERMISSIONS: BOT_PERMISSIONS,
  SUBSCRIPTION_PERMISSIONS: SUBSCRIPTION_PERMISSIONS,
};
